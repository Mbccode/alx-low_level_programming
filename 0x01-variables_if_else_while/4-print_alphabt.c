#include <stdio>

/**
 * main - Entry point
 * Description: Prints all letters except q and e
 * Return: 0
 */

int main(void)
{
	char c = 'a';
	
	while (c <= 'z' != 'q' && 'e')
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
