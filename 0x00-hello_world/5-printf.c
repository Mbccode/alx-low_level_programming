#include <stdio.h>

/**
 * main - entry point
 *
 * return: Returns 0 if exited in proper manner
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
